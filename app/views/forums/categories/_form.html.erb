<%= form_for(@forums_category) do |f| %>
  <% if @forums_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forums_category.errors.count, "error") %> prohibited this forums_category from being saved:</h2>

      <ul>
      <% @forums_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>