<h1>Listing forums_categories</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @forums_categories.each do |forums_category| %>
  <tr>
    <td><%= forums_category.name %></td>
    <td><%= link_to 'Show', forums_category %></td>
    <td><%= link_to 'Edit', edit_forums_category_path(forums_category) %></td>
    <td><%= link_to 'Destroy', forums_category, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Category', new_forums_category_path %>
